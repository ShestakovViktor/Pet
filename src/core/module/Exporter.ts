/*
import Visitor from './visitor.js';

export default function Exporter() {
	function exp() {}
	function doForEntity({ color, model, position, transform }) {
		return { color, model: model.hash, position, transform };
	}

	return Object.freeze(Object.assign({}, Visitor(), { doForEntity }));
}
*/
